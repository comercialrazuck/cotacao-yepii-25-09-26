const { createClient } = require('@supabase/supabase-js');
function decodeDataUrl(dataUrl){const m=String(dataUrl||'').match(/^data:(image\/(?:jpeg|png|webp));base64,(.+)$/);if(!m)return null;const ext=m[1]==='image/png'?'png':m[1]==='image/webp'?'webp':'jpg';return {mime:m[1],ext,buffer:Buffer.from(m[2],'base64')}}
function money(v){const n=Number(v||0);return Number.isFinite(n)?Math.round(n*100)/100:0}
exports.handler=async(event)=>{try{if(event.httpMethod!=='POST')return{statusCode:405,body:JSON.stringify({error:'Method not allowed'})};const auth=(event.headers.authorization||event.headers.Authorization||'').replace(/^Bearer\s+/i,'');if(!auth)return{statusCode:401,body:JSON.stringify({error:'Authentication required'})};const authClient=createClient(process.env.SUPABASE_URL,'sb_publishable_9OnuKQOmBQR7TArHu3-X7g_HXbQpQoc',{auth:{persistSession:false,autoRefreshToken:false}});const {data:{user},error:userErr}=await authClient.auth.getUser(auth);if(userErr||!user)return{statusCode:401,body:JSON.stringify({error:'Sessão inválida'})};const supabase=createClient(process.env.SUPABASE_URL,process.env.SUPABASE_SECRET_KEY,{auth:{persistSession:false,autoRefreshToken:false}});const body=JSON.parse(event.body||'{}'),q=body.quote||{},items=Array.isArray(q.items)?q.items:[];if(!q.quote_number)throw new Error('Informe o número da cotação.');if(!items.length)throw new Error('Adicione pelo menos um produto.');const subtotal=items.reduce((s,i)=>s+money(i.quantity)*money(i.unit_price),0),total=money(subtotal+money(q.tax)+money(q.other_amount)+money(q.freight));let quoteId=body.quote_id||null,publicToken=null;const quoteRow={quote_number:q.quote_number,status:'sent',quote_date:q.quote_date||null,client_id:q.client_id||null,company:q.company||null,cnpj:q.cnpj||null,address:q.address||null,phone:q.phone||null,tax:money(q.tax),other_amount:money(q.other_amount),freight:money(q.freight),delivery_terms:q.delivery_terms||null,seller:q.seller||null,payment_terms:q.payment_terms||null,notes:q.notes||null,notify_email_1:q.notify_email_1||'cotacaodireta@yepii.com.br',notify_email_2:q.notify_email_2||null,notify_email_3:q.notify_email_3||null,notify_whatsapp:q.notify_whatsapp||null,subtotal:money(subtotal),total,sent_at:new Date().toISOString()};if(quoteId){const {data,error}=await supabase.from('quotes').update(quoteRow).eq('id',quoteId).select('id,public_token').single();if(error)throw error;publicToken=data.public_token;await supabase.from('quote_items').delete().eq('quote_id',quoteId)}else{const {data,error}=await supabase.from('quotes').insert(quoteRow).select('id,public_token').single();if(error)throw error;quoteId=data.id;publicToken=data.public_token;await supabase.from('quote_events').insert({quote_id:quoteId,event_type:'created'})}const rows=[];for(let idx=0;idx<items.length;idx++){const i=items[idx];let imageUrl=i.image_url||null;if(i.image_data){const dec=decodeDataUrl(i.image_data);if(dec){const path=`${quoteId}/${crypto.randomUUID()}.${dec.ext}`;const {error}=await supabase.storage.from('quote-images').upload(path,dec.buffer,{contentType:dec.mime,upsert:false});if(error)throw error;imageUrl=supabase.storage.from('quote-images').getPublicUrl(path).data.publicUrl}}rows.push({quote_id:quoteId,position:idx+1,description:String(i.description||'').trim(),quantity:money(i.quantity||1),unit_price:money(i.unit_price),image_url:imageUrl,selected:i.selected!==false})}const {error:itemErr}=await supabase.from('quote_items').insert(rows);if(itemErr)throw itemErr;await supabase.from('quote_events').insert({quote_id:quoteId,event_type:'sent'});return{statusCode:200,headers:{'Content-Type':'application/json'},body:JSON.stringify({ok:true,quote_id:quoteId,public_token:publicToken})}}catch(e){return{statusCode:500,headers:{'Content-Type':'application/json'},body:JSON.stringify({error:e.message||'Erro inesperado'})}}};