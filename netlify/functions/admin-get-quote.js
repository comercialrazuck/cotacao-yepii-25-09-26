const { createClient } = require('@supabase/supabase-js');
const PUBLISHABLE_KEY='sb_publishable_9OnuKQOmBQR7TArHu3-X7g_HXbQpQoc';
function json(statusCode,body){return{statusCode,headers:{'Content-Type':'application/json'},body:JSON.stringify(body)}}
exports.handler=async(event)=>{try{if(event.httpMethod!=='GET')return json(405,{error:'Method not allowed'});const token=String(event.headers.authorization||event.headers.Authorization||'').replace(/^Bearer\s+/i,'');if(!token)return json(401,{error:'Authentication required'});const authClient=createClient(process.env.SUPABASE_URL,PUBLISHABLE_KEY,{auth:{persistSession:false,autoRefreshToken:false}});const {data:{user},error:userErr}=await authClient.auth.getUser(token);if(userErr||!user)return json(401,{error:'Sessão inválida'});const id=event.queryStringParameters?.id;if(!id)return json(400,{error:'Cotação inválida'});const supabase=createClient(process.env.SUPABASE_URL,process.env.SUPABASE_SECRET_KEY,{auth:{persistSession:false,autoRefreshToken:false}});const {data:q,error:qErr}=await supabase.from('quotes').select('*').eq('id',id).single();if(qErr)throw qErr;const {data:items,error:iErr}=await supabase.from('quote_items').select('id,position,description,quantity,unit_price,line_total,image_url,selected').eq('quote_id',id).order('position');if(iErr)throw iErr;const {data:events,error:eErr}=await supabase.from('quote_events').select('id,event_type,event_data,created_at').eq('quote_id',id).order('created_at',{ascending:true});if(eErr)throw eErr;const {data:acceptance}=await supabase.from('quote_acceptances').select('approver_name,approver_role,approver_email,approver_phone,accepted_total,accepted_at,terms_version').eq('quote_id',id).order('accepted_at',{ascending:false}).limit(1).maybeSingle();return json(200,{quote:{...q,items:items||[]},events:events||[],acceptance:acceptance||null})}catch(e){return json(500,{error:e.message||'Erro inesperado'})}};
