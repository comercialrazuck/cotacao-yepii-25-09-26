const { createClient } = require('@supabase/supabase-js');
function money(v){const n=Number(v||0);return Number.isFinite(n)?Math.round(n*100)/100:0}
exports.handler=async(event)=>{try{if(event.httpMethod!=='POST')return{statusCode:405,body:JSON.stringify({error:'Method not allowed'})};const b=JSON.parse(event.body||'{}');if(!b.token)throw new Error('Token ausente');if(!b.approver_name||!b.approver_email||!b.approver_phone)throw new Error('Dados do responsável incompletos');const positions=Array.isArray(b.selected_positions)?b.selected_positions.map(Number).filter(Boolean):[];if(!positions.length)throw new Error('Selecione pelo menos um item');const supabase=createClient(process.env.SUPABASE_URL,process.env.SUPABASE_SECRET_KEY);const {data:q,error}=await supabase.from('quotes').select('*').eq('public_token',b.token).single();if(error||!q)return{statusCode:404,body:JSON.stringify({error:'Cotação não encontrada'})};if(['expired','cancelled'].includes(q.status))return{statusCode:410,body:JSON.stringify({error:'Cotação indisponível'})};if(q.status==='accepted')return{statusCode:409,body:JSON.stringify({error:'Cotação já aceita'})};const {data:items,error:itemErr}=await supabase.from('quote_items').select('id,position,description,quantity,unit_price,line_total').eq('quote_id',q.id);if(itemErr)throw itemErr;const selected=(items||[]).filter(i=>positions.includes(i.position));if(!selected.length)throw new Error('Nenhum item válido selecionado');const subtotal=selected.reduce((s,i)=>s+money(i.line_total),0),acceptedTotal=money(subtotal+money(q.tax)+money(q.other_amount)+money(q.freight));for(const i of (items||[]))await supabase.from('quote_items').update({selected:positions.includes(i.position)}).eq('id',i.id);const acceptedAt=new Date().toISOString();const {error:accErr}=await supabase.from('quote_acceptances').insert({quote_id:q.id,approver_name:b.approver_name,approver_role:b.approver_role||null,approver_email:b.approver_email,approver_phone:b.approver_phone,accepted_total:acceptedTotal,selected_items:selected,user_agent:event.headers['user-agent']||null,accepted_at:acceptedAt});if(accErr)throw accErr;await supabase.from('quotes').update({status:'accepted',accepted_at:acceptedAt,subtotal,total:acceptedTotal}).eq('id',q.id);await supabase.from('quote_events').insert({quote_id:q.id,event_type:'accepted',event_data:{accepted_total:acceptedTotal,approver_email:b.approver_email}});return{statusCode:200,headers:{'Content-Type':'application/json'},body:JSON.stringify({ok:true,accepted_total:acceptedTotal,accepted_at:acceptedAt})}}catch(e){return{statusCode:500,body:JSON.stringify({error:e.message||'Erro inesperado'})}}};